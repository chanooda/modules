import { IInputs } from './FormHook';

function App() {
  const inputs: IInputs[] = [
    { key: 'id', label: '아이디', type: 'text', isButton: true },
  ];

  return <div></div>;
}

export default App;
